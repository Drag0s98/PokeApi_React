import React from "react";

const Card = () => {
  return (
    <section>
      <form action="">
        <input type="text" />
        <button>Search</button>
      </form>
    </section>
  )
};

export default Card;
